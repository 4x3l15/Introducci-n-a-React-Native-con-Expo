import { View, Text } from "react-native";

export default function HolaMundo() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 30, color: "green" }}>Hola React Native</Text>
    </View>
  );
}
